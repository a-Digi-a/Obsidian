#programming #programming/bash 

# Bash Parameter Expansion

In **bash**, **parameter expansion** allows us to **substitute a parameter reference with its value**. This is useful for modifying the parameter in some way in 