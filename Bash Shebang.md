#programming #programming/bash 

# Bash Shebang

The **shebang** (*#!*) is the first line in a bash script. It tells the computer where to look for the **bash program** so it can use it to run the script. Here is an example:

```bash
#!/bin/bash
echo Hello, World!
```