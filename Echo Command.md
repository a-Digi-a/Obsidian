#programming #programming/bash 

# Echo

**echo** is the equivalent of a **print statement** in **bash**. It takes in **arguments** and **prints them to the command line**:

```bash
echo Hello, World!
```