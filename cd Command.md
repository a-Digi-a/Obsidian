#programming #programming/bash 

# cd

The **cd** command will **change the current working directory** to the **argument** passed in. If **no argument** is passed into **cd** it will **change the directory to the current users home directory**.

```bash
cd
pwd
cd Downloads
pwd
```